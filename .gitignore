node_modules/
dist/
.pnpm-store/
.DS_Store
*.log
Download-on-the-App-Store/
Get it on Google Play Badges/

# LICENSE + NOTICE are synced into each package by scripts/sync-license.mjs
# (run via `prepublishOnly`). The canonical copies live at the repo root.
packages/*/LICENSE
packages/*/NOTICE
